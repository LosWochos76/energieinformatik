<Window x:Class="TimeSeriesGUI.TimeSeriesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeSeriesGUI"
        mc:Ignorable="d"
        Title="TimeSeriesDialog" SizeToContent="WidthAndHeight">
    <StackPanel>
        <Grid Margin="5 5 5 5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top">Name:</Label>
            <TextBox Grid.Column="1" Grid.Row="0" MinWidth="200" Text="{Binding Name}" VerticalAlignment="Center" HorizontalAlignment="Stretch" />
            <WrapPanel Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,5,0,0">
                <Button IsDefault="True" MinWidth="60" Margin="0,0,10,0" Click="Ok_Click">_Ok</Button>
                <Button IsCancel="True" MinWidth="60" Click="Cancel_Click">_Abbrechen</Button>
            </WrapPanel>
        </Grid>
    </StackPanel>
</Window>
